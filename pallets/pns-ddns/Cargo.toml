[package]
name = "pns-ddns"
version = "0.1.0"
edition = "2024"

[dependencies]
codec     = { workspace = true }
tracing   = { workspace = true }
pns-types = { path = "../pns-types", features = ["std"] }
