{
  "pns": {
    "getInfo": {
      "description": "Get full name record by namehash (H256). Returns owner, expiry, capacity, fee, for_sale, last_block, and the block at which the record was read.",
      "params": [
        { "name": "node", "type": "H256" }
      ],
      "type": "Option<Json>"
    },
    "resolveName": {
      "description": "Resolve a plain label (e.g. 'alice') to its full name record. Returns null if the name does not exist or is expired.",
      "params": [
        { "name": "name", "type": "Text" }
      ],
      "type": "Option<Json>"
    },
    "lookup": {
      "description": "Get DNS records stored for a namehash (H256), filtered by record type codes. Returns an array of [record_type_code, bytes] pairs.",
      "params": [
        { "name": "node", "type": "H256" },
        { "name": "record_types", "type": "Vec<u32>" }
      ],
      "type": "Vec<(u32, Bytes)>"
    },
    "lookupByName": {
      "description": "Get DNS records for a plain label or dotted name (e.g. 'alice' or 'sub.alice'), filtered by record type codes. Returns an array of [record_type_code, bytes] pairs.",
      "params": [
        { "name": "name", "type": "Text" },
        { "name": "record_types", "type": "Vec<u32>" }
      ],
      "type": "Vec<(u32, Bytes)>"
    },
    "getListing": {
      "description": "Get the active marketplace listing for a plain label (e.g. 'alice'). Returns seller, price, expires_at, and the block at which the listing was read. Returns null if not listed.",
      "params": [
        { "name": "name", "type": "Text" }
      ],
      "type": "Option<Json>"
    },
    "all": {
      "description": "Return every registered name and its registrar info as an array of [namehash, info] pairs. Intended for indexers and explorers.",
      "params": [],
      "type": "Vec<(H256, Json)>"
    },
    "accountDashboard": {
      "description": "Return the full name portfolio for an account in a single call. Includes: primary_name (H256 or null), subnames (array of H256), pending_subname_offers (array of H256), and pending_name_offers (array of H256).",
      "params": [
        { "name": "account", "type": "AccountId" }
      ],
      "type": "Json"
    }
  }
}
